import React from 'react';
import classes from './Navigation.module.scss';

const Navigation = () => {
    return <section className={classes.wrapper}>Navigation</section>;
};

export default Navigation;
